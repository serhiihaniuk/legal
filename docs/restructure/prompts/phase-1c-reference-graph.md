You are implementing Phase 1 (part C: the reference graph and lifecycle proof) — the final pipeline-hardening slice. Read first: docs/restructure/PLAN.md "Phase 1" items 4, 6, 7 and the "Decisions resolved at gates" section, then docs/restructure/evidence/pipeline-map.report.md Problems ("Dependant discovery is promised but not implemented"). Phases 1a and 1b are already on this branch's base — study the current scripts/legal-corpus/lib/workflow.mjs (promoteEdition, prepareWorkOrder, dry-run, force-rebuild) before designing.

Scope: scripts/legal-corpus/**, scripts/legal-editorial/validate.mjs (one small task below), docs/restructure/citation-review/ (generated packet), and this prompt file. Never touch app/ source; READING app/data/** is expected — the scanner's subject is those files.

Tasks, one conventional commit each (commit this prompt file first as "docs: add phase 1c prompt"):

1. **Typed-reference scanner** (new lib module): statically scan the authored content files that carry typed citations — app/data/legal-library/editorial/**/articles/article-*.ts, app/data/legal-library/learning/*.ts, app/data/case-guides/routes.ts, app/data/legal-map/node-guides-*.ts, app/data/legal-map/data.ts — and extract every `.article("N", ...)` / `.articleRange("A","B", ...)` call with its author document (resolve the author variable from its `createLegalTextAuthor("<documentId>")` declaration in the same file). Output: a map of provisionId -> [{file, line}]. Static text scanning is acceptable (the call shapes are uniform); document any pattern it cannot see.

2. **Dependant discovery wired into the lifecycle**: prepare (update mode) and the standalone diff fill `reviewDependants` from the scanner — for every changed or removed provision, the citing files/lines that a reviewer must inspect. The work-order packet renders them in its markdown. This closes the evidence report's HIGH "dependant discovery promised but not implemented".

3. **Citation attribution audit packet**: a new workflow subcommand `audit-citations` that uses the scanner plus the reference registry to emit docs/restructure/citation-review/ambiguous-citations.md: every citation whose article number exists in MORE THAN ONE registered act, grouped by file, with the chosen act and the competing acts. Include all scanned files, but mark the codemod-transformed families (legal-data.ts, legal-node-guides-*.ts) as the priority review set — their attribution was heuristic. Run it once and commit the generated packet.

4. **Lifecycle integration test**: a full prepare -> validate -> promote pass against a small fixture document with an injected fetch (no network; add a fetch seam to the builder if one does not exist — keep the default untouched). Must cover: fresh build + publication layout; --dry-run touches nothing; a tampered provisions.json fails promotion (1a's re-verification); --force-rebuild + unchanged checksum preserves builtAt (1b); the promotion write order and promotion record. Use temp directories via the existing projectRoot parameters. Follow the existing node --test style.

5. **KPA exclusion becomes loud**: in scripts/legal-editorial/validate.mjs, replace the silent `if (documentId === "kpa") continue` with the same skip PLUS an explicit reported warning line stating KPA editorial coverage is not validated pending the Phase 2 migration. Do not otherwise change that validator; keep your edit clean under checkJs without fixing its 32 pre-existing diagnostics.

Gates at the end: npm run verify green AND npm run test:corpus green. New/edited corpus code stays at zero typecheck:scripts diagnostics. Do not push.

Final summary: commits; scanner coverage stats (files scanned, citations found, provisions covered); dependants example for one provision; ambiguous-citation counts (total and priority set); integration-test scenarios covered; verification results; anything deferred.
